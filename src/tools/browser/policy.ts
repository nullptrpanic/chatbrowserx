/** Shared Browser scope decisions contributed by the Browser tool category. */
export const BROWSER_EXECUTION_POLICY =
  'Browser evidence scope: coverage.complete=false requires traversal only for whole-document, all-items, date-range, or page-summary requests; it never makes a named section incomplete. Current-page evidence MUST come from Browser tools. For page-wide reading, use the primary scroll surface, never same-page or table-of-contents links; sample and disclose a bounded range for open-ended feeds. Preserve source headings and key terms when summarizing. An exact same-page or table-of-contents link may locate a named section but does not prove its coverage. Reuse task metadata and verified post-action evidence; call another browser tool only when it can supply evidence still required by the request. For locate, open, or view requests, inspect the target and enough context, then stop. Read farther only for explicit broader analysis. For a complete named section, use the next same-level or higher-level heading as stopText only when explicit heading levels or parent relationships prove hierarchy; accessibility-tree depth and table-of-contents order do not. Otherwise maxSegments MUST be 1 with stopText="" and reassess each observation. A target heading followed immediately by another heading before body text is a likely section container: continue through its apparent subsections until a plausible peer container appears. Otherwise stop at the first plausible following section. Never pass a second plausible peer, and omit adjacent overlap from the answer. Never seek a distant marker or document boundary; empty stopText may reach the boundary only when inspection shows the requested section is final.';
